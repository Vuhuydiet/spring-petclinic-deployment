apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spring-petclinic-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.host }}
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: "admin-server"
            port:
              number: {{ .Values.adminServer.port }}
      - path: /
        pathType: Prefix
        backend:
          service:
            name: "api-gateway"
            port:
              number: {{ .Values.apiGateway.port }}
    